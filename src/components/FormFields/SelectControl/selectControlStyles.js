import { css } from 'styled-components';
// import breakpoint from 'styled-components-breakpoint';console.log(props)
const selectControlStyles = css`
    position: relative;
`;

export default selectControlStyles;
