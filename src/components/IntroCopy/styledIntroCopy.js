import styled from 'styled-components';
import introCopyStyles from './introCopyStyles';

// NOTE: change yourElementType to your preferred type: e.g button
const StyledIntroCopy = styled.div`
  ${ introCopyStyles };
`;

export default StyledIntroCopy;